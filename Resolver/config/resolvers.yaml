{
  "resolvers": [
    {
      "ecosystem": "pip",
      "mode": "process",
      "command": [
        "${PYTHON}",
        "Resolver/pip-dependency-resolver/adapter_main.py"
      ],
      "workdir": ".",
      "timeout_ms": 60000,
      "capabilities": {
        "commands": ["resolve", "health", "capabilities"],
        "formats": ["graph"],
        "features": ["raw", "markers", "extras"]
      }
    },
    {
      "ecosystem": "maven",
      "mode": "process",
      "command": [
        "${PYTHON}",
        "Resolver/maven-dependency-resolver/adapter_launcher.py"
      ],
      "workdir": ".",
      "timeout_ms": 120000,
      "capabilities": {
        "commands": ["resolve", "health", "capabilities"],
        "formats": ["graph"],
        "features": ["raw", "scopes", "managed-dependencies"]
      }
    },
    {
      "ecosystem": "go",
      "mode": "process",
      "command": [
        "${PYTHON}",
        "Resolver/go-dependency-resolver/adapter_launcher.py"
      ],
      "workdir": ".",
      "timeout_ms": 120000,
      "capabilities": {
        "commands": ["resolve", "health", "capabilities"],
        "formats": ["graph"],
        "features": ["raw", "replace", "exclude", "buildlist"]
      }
    },
    {
      "ecosystem": "cargo",
      "mode": "process",
      "command": [
        "${PYTHON}",
        "Resolver/cargo-dependency-resolver/adapter_launcher.py"
      ],
      "workdir": ".",
      "timeout_ms": 120000,
      "capabilities": {
        "commands": ["resolve", "health", "capabilities"],
        "formats": ["graph", "full"],
        "features": ["raw", "features", "registry"]
      }
    },
    {
      "ecosystem": "npm",
      "mode": "process",
      "command": [
        "${PYTHON}",
        "Resolver/js-dependency-resolver/adapter_launcher.py"
      ],
      "workdir": ".",
      "timeout_ms": 120000,
      "capabilities": {
        "commands": ["resolve", "health", "capabilities"],
        "formats": ["graph"],
        "features": ["raw", "peer-dependencies", "directory-tree"]
      }
    }
  ]
}
