# Crawler Placeholder

This directory is intentionally kept as a placeholder.
It is not part of the current active OpenDep runtime path.

## Intended role

Future crawler implementations can be organized here after the resolver-first architecture is stable.
Until then, the recommended entrypoint for end users is the repository-root `main.py` file and the active implementation under `Resolver/`.
