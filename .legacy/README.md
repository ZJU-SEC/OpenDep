# OpenDep Legacy Area

This directory is reserved for deprecated, archived, or exploratory material that is not part of the current recommended workflow.

## Usage policy

- Do not treat `.legacy/` as a supported runtime entrypoint.
- Prefer the active workspace under `Resolver/`.
- Prefer the repository-root `main.py` CLI for day-to-day usage.
