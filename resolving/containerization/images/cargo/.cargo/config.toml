[net]
git-fetch-with-cli = true

[source.mirror]
# The resolver runtime stages a local-registry layout under the dedicated Cargo runtime root.
local-registry = "/opt/opendep/cargo-runtime/local-registry"

[source.crates-io]
replace-with = "mirror"
