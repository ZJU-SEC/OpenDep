[net]
git-fetch-with-cli = true

[source.mirror]
# Indexed mode consumes the preprocess-managed local-registry snapshot.
local-registry = "/cargo-data/local-registry"

[source.crates-io]
replace-with = "mirror"
