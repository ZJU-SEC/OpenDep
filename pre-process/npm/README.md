# npm Pre-process Workspace

This workspace is for JavaScript / Node.js package preprocessing and database-loading logic.

- `adapters/`: adapt outputs from the existing `npm` or legacy `js` paths
- `pipeline/`: clean and normalize npm package metadata and dependency edges
- `loaders/`: write processed `npm` data into the database
