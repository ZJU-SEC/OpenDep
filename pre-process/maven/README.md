# Maven Pre-process Workspace

This workspace is for Java package preprocessing and database-loading logic.

- `adapters/`: adapt outputs from the existing `maven` crawler or resolver paths
- `pipeline/`: clean and normalize Maven coordinates, metadata, and dependency edges
- `loaders/`: write processed `maven` data into the database
