# pip Pre-process Workspace

This workspace is for Python package preprocessing and database-loading logic.

- `adapters/`: adapt outputs from the existing `pip` crawler or resolver paths
- `pipeline/`: clean and normalize Python package metadata and dependency edges
- `loaders/`: write processed `pip` data into the database
