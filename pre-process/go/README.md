# Go Pre-process Workspace

This workspace is for Go module preprocessing and database-loading logic.

- `adapters/`: adapt outputs from the existing `go` crawler or resolver paths
- `pipeline/`: clean and normalize module metadata and dependency edges
- `loaders/`: write processed `go` data into the database
